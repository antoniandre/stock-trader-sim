# Trading Dashboard (Vue 3 + Vite)

## Setup
```bash
cd frontend
pnpm install
pnpm dev
```
The app runs on **localhost:5173** and expects the backend on `localhost:3000`.

## Build
```bash
pnpm build
```